name: vet_planner
description: A VetPlanner applications.
publish_to: 'none'

version: 0.0.1

environment:
  sdk: ^3.1.3
  flutter: ^3.13.6

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ./packages/core
  navigation:
    path: ./packages/navigation


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
